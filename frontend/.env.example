# Copy to .env only if you are NOT using the nginx proxy (docker) or the
# vercel.json rewrite. In those setups the app calls same-origin "/api" and
# you can leave this unset.
#
# Direct cross-origin call to the backend instead:
# VITE_API_BASE=https://your-backend.onrender.com/api
